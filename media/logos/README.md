Drop real institution logos here as `<id>.svg` and set `hasLogo: true` on the
matching entry in components/Affiliations.tsx.

Expected ids: tno, uu, csir, isro, ieee

They are rendered at 20px tall, grayscale, at 70% opacity, so they sit inside
the page palette rather than competing with it. Use official files only, and
prefer wordmarks over emblems at this size.
